/**
 * Delay (ms) before a tooltip opens on hover. Far shorter than the
 * browser's native `title` delay (~1s), so hints feel instant — yet long
 * enough to avoid flicker as the pointer sweeps across the page.
 */
export const TOOLTIP_OPEN_DELAY = 80;
